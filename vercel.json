{
  "buildCommand": "cd frontend && npm install && chmod +x node_modules/.bin/* && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install",
  "framework": "vite"
}
